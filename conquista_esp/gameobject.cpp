#include "gameobject.h"

GameObject::GameObject() {
    //setPixmap(QPixmap(imagePath));
    /*object = new sprite(imagePath, limite, columnas);
    object->setParentItem(this);
    setRect(-31, -49, 40, 40); // Establece el tamaño del jugador
    object->setfilas(0);*/
    //object->setY(20);
    //setPen(Qt::NoPen);
}
